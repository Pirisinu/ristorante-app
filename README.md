# Il Ramo D'oro — Sito (Fase 1)

Stack: React + Vite + React-Bootstrap, nessun backend.

## Comandi

```bash
npm install
npm run dev       # sviluppo, http://localhost:5173
npm run build     # build di produzione in /dist
npm run preview   # anteprima della build
```

